class Solution:
    def duplicateZeros(self, arr: List[int]) -> None:
        lst  = []
        for num in arr:
            if num == 0:
                lst.append(num)
                lst.append(0)
            else:
                lst.append(num)
        arr[:] = lst[:len(arr)]
